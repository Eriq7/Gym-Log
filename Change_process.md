# 自定义打卡动作

**2025-09-13** · commit `5d23cd7`

把写死的动作列表变成用户可增删改的。

---

## 改了什么

动作列表原本是 JS 里的 `const SECTIONS`，现在搬进 `state.sections`，存 localStorage。

```js
// before                          // after
{ startDate, logs }                { startDate, sections, logs }
```

`sections[i]` = `{ id, name, note?, per, mode }`，`mode` 是 `'week'`（每周 N 次）或 `'count'`（累计 N 次一轮）。

## 界面

| 入口 | 位置 |
|---|---|
| 新增 | 列表底部虚线卡「＋ 新增动作」 |
| 编辑 | 每张卡右上角 `⋯` |

面板里可改：名称、备注、周期、卡片数（1–50）、删除。

## 几个决策

**卡片数改动会重算周期。** 打卡记录存的是一串扁平日期，「第几周」是按 `per` 切出来的，不是存的。所以改 `per` 会重新分组：

```
8 条记录，per 3 → [1 2 3][4 5 6][7 8]  = 2 周 + 2/3
        per 5 → [1 2 3 4 5][6 7 8]     = 1 周 + 3/5
```

记录一条不丢，但大数字会变。面板里**保存前**就红框预告这个变化。

> 备选方案是「历史冻结」（存周期边界，大数字只涨不跌），因为撤销退回切换点时逻辑容易出 bug，没采用。

**格子排版改成看卡片数，不看模式。** ≤7 张 → 3 列大格子，>7 张 → 10 列小格子（窄屏 5 列）。原来的 4 张卡长相不变。

**「每周」仍不按真实日历。** 第几周 = `floor(打卡数 / per) + 1`，点满才进下一周，周一不会自动清空。这是原有行为，本次没动。

**删除是真删。** 弹窗写明记录条数，确认后 section 和 logs 一起清掉。

## 迁移

老数据自动升级，seed 的默认动作沿用原 id（`chest` / `back` / `shoulder` / `abs`），`fitness_stamp_v1` 这个 key 没变 —— **已有打卡记录不受影响**。

## 顺带

- 卡片入场动画改成 opt-in（`.enter`），否则每次保存整页都会重抖一遍
- 底部说明去掉了写死的规则文案（「胸背满 20…」），改成通用的「点满自动进下一轮」

## 测过

本地起服务实跑：老数据迁移、新增、改名、换周期、改卡片数（含 999/0/空/负数/字母的边界）、7→8 张的排版切换、删除、Esc 分层、全删空、满格翻页与撤销、390px 窄屏、Console 无报错。

## 没做

拖拽排序 —— 新动作一律追加在末尾。
